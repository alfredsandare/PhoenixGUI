from PhoenixGUI import Image
from PhoenixGUI import RenderedMenuObject
from PhoenixGUI.util import compare_objects
import pygame

'''
def test_image_attrs():
    image = Image((100, 80), pygame.Surface((30, 40)))
    assert image.pos == ((100, 80))
    #assert image.image == pygame.Surface((30, 40))
    assert compare_objects(image.image, pygame.Surface((30, 40)))
'''